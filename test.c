#pragma warning(disable:4996)
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(){
	char buf[20];
    char buf2[20]
	int i;

	gets(buf);
    gets(buf2)

	i = atoi(buf);

	printf("%d\n", i);

	// system("/bin/sh");
	system(buf);


	return 0;
}